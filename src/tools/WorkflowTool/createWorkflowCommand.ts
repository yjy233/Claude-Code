export function createWorkflowCommand() {
  return null
}

// Restored placeholder: commands.ts expects this export for cache invalidation.
export function getWorkflowCommands(_cwd: string) {
  return Promise.resolve([])
}
